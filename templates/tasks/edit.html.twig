{% extends 'base.html.twig' %}

{% block title %}Edit Tasks
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Edit Tasks</h1>

		{{ include('tasks/_form.html.twig', {'button_label': 'Update'}) }}

		<a href="{{ path('app_tasks_index') }}">back to list</a>

		{{ include('tasks/_delete_form.html.twig') }}
	</div>
{% endblock %}
